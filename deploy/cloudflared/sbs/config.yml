# Generated by tools/generate_deploy_artifacts.py from deployments/sbs.yml.
# Edit the deployment file, not this one.
#
tunnel: 8dd766e0-8396-4465-b428-4bd0769025f0
credentials-file: /Users/william/.cloudflared/8dd766e0-8396-4465-b428-4bd0769025f0.json

ingress:
  # One rule. The gateway owns every path on this hostname: the SPA,
  # /config.json, /<app>/ws (proxied in-process), /image/<app>/ and
  # /guider/<name>/. Adding an instrument does not touch this file.
  - hostname: sbs.chimera.observer
    service: http://localhost:8080
  # required catch-all
  - service: http_status:404
